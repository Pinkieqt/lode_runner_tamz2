package com.loderunner.game;

import android.graphics.Canvas;

public class WallBlock extends BaseBlock{
    public WallBlock(int topPosX, int topPosY, int size, Canvas canvas) {
        super(topPosX, topPosY, size, canvas);
    }
}
