package com.loderunner.game;

import android.graphics.Canvas;

public class LadderBlock extends BaseBlock {
    public LadderBlock(int topPosX, int topPosY, int size, Canvas canvas) {
        super(topPosX, topPosY, size, canvas);
    }
}
